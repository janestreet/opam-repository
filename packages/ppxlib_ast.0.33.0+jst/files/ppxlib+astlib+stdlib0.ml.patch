--- a/astlib/stdlib0.ml
+++ b/astlib/stdlib0.ml
@@ -3,5 +3,6 @@ module Int = struct
 end
 
 module Option = struct
+  let is_none o = match o with None -> true | Some _ -> false
   let map f o = match o with None -> None | Some v -> Some (f v)
 end
