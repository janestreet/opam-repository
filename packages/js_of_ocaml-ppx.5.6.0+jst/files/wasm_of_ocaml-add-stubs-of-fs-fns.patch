--- a/runtime/wasm/fs.wat
+++ b/runtime/wasm/fs.wat
@@ -119,6 +119,17 @@
       (call $caml_raise_no_such_file (local.get 0))
       (ref.i31 (i32.const 0)))

+   (data $caml_create_file "caml_create_file")
+
+   (func (export "caml_create_file")
+      (param (ref eq)) (param (ref eq)) (result (ref eq))
+      ;; ZZZ
+      (call $log_str
+         (array.new_data $string $caml_create_file
+            (i32.const 0) (i32.const 16)))
+      (call $caml_raise_no_such_file (local.get 0))
+      (ref.i31 (i32.const 0)))
+
    (func (export "caml_fs_init") (result (ref eq))
       (ref.i31 (i32.const 0)))

@@ -130,4 +141,13 @@
          (array.new_data $string $caml_sys_is_directory
             (i32.const 0) (i32.const 21)))
       (ref.i31 (i32.const 0)))
+
+   (data $caml_mount_autoload "caml_mount_autoload")
+
+   (func (export "caml_mount_autoload") (param (ref eq)) (param (ref eq)) (result (ref eq))
+      ;; ZZZ
+      (call $log_str
+         (array.new_data $string $caml_mount_autoload
+            (i32.const 0) (i32.const 19)))
+      (ref.i31 (i32.const 0)))
 )
